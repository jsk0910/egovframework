<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.example.dca.service.OAuthMapper">
	<!-- findByEmail in Kakao -->
	<select id="findInKakao" parameterType="java.util.HashMap" resultType="memberVO">
		<![CDATA[SELECT * FROM member WHERE Email=#{email}]]>
	</select>
</mapper>